using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Kapı_Move : MonoBehaviour
{
    //public Transform platform;
    //public Transform startPoint;
    //public Transform endPoint;
    //public float speed = 1.5f;
    //private bool hasReachedEndPoint = false;

    //private void Update()
    //{
    //    if (!hasReachedEndPoint)
    //    {
    //        Vector3 target = endPoint.position;
    //        platform.position = Vector3.Lerp(platform.position, target, speed * Time.deltaTime);

    //        float distance = (target - platform.position).magnitude;

    //        if (distance <= 0.1f)
    //        {
    //            hasReachedEndPoint = true;
    //            platform.position = endPoint.position; // Ensure it exactly reaches the endpoint
    //        }
    //    }
    //}

    //private void OnDrawGizmos()
    //{
    //    if (platform != null && startPoint != null && endPoint != null)
    //    {
    //        Gizmos.DrawLine(platform.position, startPoint.position);
    //        Gizmos.DrawLine(platform.position, endPoint.position);
    //    }
    //}
}
